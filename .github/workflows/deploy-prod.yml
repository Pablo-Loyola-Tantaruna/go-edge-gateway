name: "GopherGuard - Pipeline PROD"

on:
  push:
    tags: [ 'v[0-9].[0-9].[0-9]' ] # Ejemplo: v1.0.0

jobs:
  Fast-Delivery:
    runs-on: ubuntu-latest
    environment: production
    steps:
      - name: Validate Artifact
        run: echo "Verificando integridad del artefacto v${{ github.ref_name }}"

      # Despliegue de Alta Disponibilidad
      - name: Production Rollout
        run: |
          echo "Desplegando con vars/prod.yaml"
          echo "HPA Max Replicas: 10 | ResourcePlan: Gold"